generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model LicenseCategory {
  id   Int    @id @default(autoincrement())
  name String @unique // Ej: "A2", "B1", "C1"

  subjects        Subject[]
  students        Student[]
  allowedStudents AllowedStudent[]
}

model Module {
  id   Int    @id @default(autoincrement())
  name String @unique // Ej: "Módulo Teórico", "Módulo Básico"

  subjects Subject[]
}

model Subject {
  id           Int     @id @default(autoincrement())
  name         String  @unique
  description  String?
  is_practical Boolean @default(false)

  categories LicenseCategory[]

  classes  Class[]
  module   Module? @relation(fields: [moduleId], references: [id])
  moduleId Int?
}

// 2. Usuarios y Roles
model Role {
  id          Int     @id @default(autoincrement())
  name        String  @unique // ADMIN, PROFESSOR, STUDENT
  description String?
  users       User[]
}

model User {
  id              Int        @id @default(autoincrement())
  email           String     @unique
  password_hash   String
  full_name       String
  document_type   String?
  document_number String?    @unique
  role_id         Int
  role            Role       @relation(fields: [role_id], references: [id])
  student         Student?
  professor       Professor?
}

model Student {
  id                 Int               @id @default(autoincrement())
  user_id            Int               @unique
  user               User              @relation(fields: [user_id], references: [id])
  license_categories LicenseCategory[]

  reservations Reservation[]
}

model Professor {
  id      Int     @id @default(autoincrement())
  user_id Int     @unique
  user    User    @relation(fields: [user_id], references: [id])
  classes Class[]
}

model WeeklySchedule {
  id         Int      @id @default(autoincrement())
  name       String
  start_date DateTime
  end_date   DateTime
  is_active  Boolean  @default(false)
  classes    Class[]
}

// 3. Programación de Clases 
model Class {
  id Int @id @default(autoincrement())

  subject_id Int
  subject    Subject @relation(fields: [subject_id], references: [id])

  class_date DateTime @db.Date
  start_time DateTime @db.Time
  end_time   DateTime @db.Time

  max_capacity       Int
  available_capacity Int

  professor_id Int
  professor    Professor @relation(fields: [professor_id], references: [id])

  reservations       Reservation[]
  status             String          @default("OPEN")
  weekly_schedule_id Int?
  weekly_schedule    WeeklySchedule? @relation(fields: [weekly_schedule_id], references: [id])
}

model Reservation {
  id         Int      @id @default(autoincrement())
  status     String   @default("ACTIVE")
  student_id Int
  student    Student  @relation(fields: [student_id], references: [id])
  class_id   Int
  class      Class    @relation(fields: [class_id], references: [id])
  created_at DateTime @default(now())
  attendance Boolean? // null = Sin confirmar, true = Asistió, false = No asistió (Falla)
}

model AllowedStudent {
  id                  Int              @id @default(autoincrement())
  document_number     String           @unique // La cédula permitida
  full_name           String? // Opcional: para saber quién es
  license_category_id Int?
  license_category    LicenseCategory? @relation(fields: [license_category_id], references: [id])
  is_registered       Boolean          @default(false) // Para saber si ya usó su cupo
  created_at          DateTime         @default(now())
}
